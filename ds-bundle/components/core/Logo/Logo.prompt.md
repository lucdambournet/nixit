Logo from nixit. Use via `window.NixItDS.Logo` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface LogoProps {
  height?: number;
  variant?: "full" | "mark";
  style?: React.CSSProperties;
}
```
