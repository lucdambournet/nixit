Avatar from nixit. Use via `window.NixItDS.Avatar` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface AvatarProps {
  src?: string | null;
  name?: string;
  size?: "xs" | "sm" | "md" | "lg" | "xl" | "2xl";
  status?: "online" | "away" | "busy" | "offline";
  style?: React.CSSProperties;
}
```
