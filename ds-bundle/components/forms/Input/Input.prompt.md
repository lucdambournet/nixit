Input from nixit. Use via `window.NixItDS.Input` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface InputProps {
  label?: string;
  error?: string;
  hint?: string;
  prefix?: React.ReactNode;
  suffix?: React.ReactNode;
  inputStyle?: React.CSSProperties;
  type?: string;
  disabled?: boolean;
  value?: string;
  placeholder?: string;
  style?: React.CSSProperties;
}
```
