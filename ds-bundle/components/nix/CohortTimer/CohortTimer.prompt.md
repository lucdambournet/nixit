CohortTimer from nixit. Use via `window.NixItDS.CohortTimer` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface CohortTimerProps {
  startDate: string;
  label?: string;
  style?: React.CSSProperties;
}
```
