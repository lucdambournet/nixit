import * as React from 'react';

/**
 * CohortTimer — from nixit@0.1.0.
 */
export interface CohortTimerProps {
  startDate: string;
  label?: string;
  style?: React.CSSProperties;
}

export declare const CohortTimer: React.ComponentType<CohortTimerProps>;
